<h1>Users index</h1>
<hr>

  <ul>
    <% @users.each do |user| %>
    <li>
      <%= user.name + ":  " + user.email %>
        <a href ="/users/<%= user.id %>">Show user</a>
    </li>
    <% end %>
    <br>
    <h3><a href="/users/new">Create new user>></a></h3>
  </ul>
